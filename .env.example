# Required to use the live extraction agent. Never commit this file after filling it.
GROQ_API_KEY=
GROQ_MODEL=groq/compound
LLM_PROVIDER=openrouter
LLM_MODEL=z-ai/glm-5.2
# Keep this in your local .env only; never commit it.
OPENROUTER_API_KEY=

# Optional: label runs that route discovery through your configured Composio MCP setup.
COMPOSIO_API_KEY=
